# Local verification reports (not OpenSpec changes)

Scratch folder for `/verify` and `/verify-fe` reports when no named change is active.

- `verify-report.md` and `verify-fe-report.md` are **gitignored** (local only).
- `.openspec.yaml` with `skip_specs: true` keeps `openspec validate --all` green.
